version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pip install -r requirements.txt
    build:
      commands:
        - echo "Construyendo aplicación..."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - .venv/**/*